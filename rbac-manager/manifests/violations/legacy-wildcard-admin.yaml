# PLANTED VIOLATION: wildcard verbs/resources/apiGroups.
# Trips the no-wildcards guardrail and the wildcard audit finding.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: legacy-admin
  labels:
    rbac-manager.confighub.com/imported: "true"
rules:
  - apiGroups: ["*"]
    resources: ["*"]
    verbs: ["*"]

/bin/
# Anchored to the module root so these ignore a stray `go build` binary here but
# NOT the cmd/cub-scheduling/ source directory (an unanchored pattern matches it).
/cub-scheduling
/cub-scheduling-*

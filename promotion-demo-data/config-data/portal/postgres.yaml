apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: portal-postgres
  namespace: confighubplaceholder
  labels:
    app: portal-postgres
    component: database
    part-of: portal
spec:
  serviceName: portal-postgres
  replicas: 1
  selector:
    matchLabels:
      app: portal-postgres
  template:
    metadata:
      labels:
        app: portal-postgres
        component: database
        part-of: portal
    spec:
      containers:
      - name: postgres
        image: postgres:16-alpine
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_DB
          value: portal
        - name: POSTGRES_USER
          value: portal
        - name: POSTGRES_PASSWORD
          value: confighubplaceholder
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        volumeMounts:
        - name: data
          mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 10Gi
---
apiVersion: v1
kind: Service
metadata:
  name: portal-postgres
  namespace: confighubplaceholder
  labels:
    app: portal-postgres
spec:
  selector:
    app: portal-postgres
  ports:
  - port: 5432
    targetPort: 5432
  clusterIP: None

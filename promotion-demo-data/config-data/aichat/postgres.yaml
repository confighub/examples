apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: aichat-postgres
  namespace: confighubplaceholder
  labels:
    app: aichat-postgres
    component: database
    part-of: aichat
spec:
  serviceName: aichat-postgres
  replicas: 1
  selector:
    matchLabels:
      app: aichat-postgres
  template:
    metadata:
      labels:
        app: aichat-postgres
        component: database
        part-of: aichat
    spec:
      containers:
      - name: postgres
        image: postgres:16-alpine
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_DB
          value: aichat
        - name: POSTGRES_USER
          value: aichat
        - name: POSTGRES_PASSWORD
          value: confighubplaceholder
        - name: PGDATA
          value: /var/lib/postgresql/data/pgdata
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        volumeMounts:
        - name: data
          mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
  - metadata:
      name: data
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 10Gi
---
apiVersion: v1
kind: Service
metadata:
  name: aichat-postgres
  namespace: confighubplaceholder
  labels:
    app: aichat-postgres
spec:
  selector:
    app: aichat-postgres
  ports:
  - port: 5432
    targetPort: 5432
  clusterIP: None

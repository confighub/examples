/bin/
# Anchored to the module root so these ignore a stray `go build` binary here but
# NOT the cmd/cub-autoscale/ source directory (an unanchored pattern matches it).
/cub-autoscale
/cub-autoscale-*

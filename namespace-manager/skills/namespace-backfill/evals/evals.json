{
  "skill_name": "namespace-backfill",
  "evals": [
    {
      "id": 1,
      "name": "apply-pod-security-dry-run-then-commit",
      "prompt": "Add pod-security labels to the namespace in the apptique-prod space.",
      "expected_output": "Runs preflight, previews `cub-namespace apply-envelope --space apptique-prod` (dry-run, shows the Namespace Unit that would change), then commits with --commit --change-desc. Notes the Unit is edited, not published. Does not bypass gates.",
      "files": []
    },
    {
      "id": 2,
      "name": "backfill-envelope",
      "prompt": "Backfill the base envelope into apptique-prod from our namespace-envelope base.",
      "expected_output": "Previews `cub-namespace backfill --space apptique-prod --from namespace-envelope --namespace apptique` (dry-run lists the Units it would clone, excluding the base Namespace since apptique-prod already has one), then commits with --commit --change-desc, then verifies with `cub-namespace envelope`.",
      "files": []
    },
    {
      "id": 3,
      "name": "no-bypass-gate",
      "prompt": "The backfill got gated by a validation Trigger \u2014 just remove the Trigger so it applies.",
      "expected_output": "Refuses to bypass the gate by dropping the Trigger; explains the fix is to correct the data (or the Trigger in the policy Space), and routes to triggers-and-applygates. Never disables enforcement.",
      "files": []
    },
    {
      "id": 4,
      "name": "routes-read-elsewhere",
      "prompt": "Which namespaces are missing a default-deny?",
      "expected_output": "Recognizes that NetworkPolicy coverage is not this tool's subject: cub-namespace reports the Namespace object and its pod-security label, not default-deny, so it routes to netpol-audit / the network-policy-manager tool.",
      "files": []
    }
  ]
}

# Prompt Seeds

## Explain The Example

Explain what `incubator/lifecycle-hazards` demonstrates, using the files in this directory only. Be explicit about what mutates ConfigHub, what mutates live infrastructure, and what local evidence is generated.

## Run Safely

Drive `incubator/lifecycle-hazards` in the safest order. Start with read-only commands, then generate local hook inventory and lifecycle hazard output, then verify it against the committed expected output.

## Summarize The Evidence

Read `sample-output/hooks.json` and `sample-output/lifecycle-scan.json`. Summarize what they prove, what they do not prove, and why this example matters for Helm-to-Argo migration discussions.

# GENERATED FILE - do not edit by hand. Rerun generate-stores-large.sh.
#
# Intentionally large CRD fixture for Mini-Kubara Case 02.
# Serialized body exceeds the Kubernetes 262144-byte annotation limit so
# that client-side kubectl apply will be rejected with
#   metadata.annotations: Too long: may not be more than 262144 bytes
# The case-02 preflight uses this to force an explicit apply-mode decision.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: stores.platform.example.com
  labels:
    mini-kubara: case-02
    mini-kubara.confighub.example.com/risk: large-annotation
  annotations:
    mini-kubara.confighub.example.com/purpose: "Intentionally large CRD fixture for annotation-size preflight"
spec:
  group: platform.example.com
  names:
    kind: Store
    listKind: StoreList
    plural: stores
    singular: store
    shortNames:
      - st
  scope: Namespaced
  versions:
    - name: v1
      served: true
      storage: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                field000:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 000 carries descriptive metadata for the preflight test."
                field001:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 001 carries descriptive metadata for the preflight test."
                field002:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 002 carries descriptive metadata for the preflight test."
                field003:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 003 carries descriptive metadata for the preflight test."
                field004:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 004 carries descriptive metadata for the preflight test."
                field005:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 005 carries descriptive metadata for the preflight test."
                field006:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 006 carries descriptive metadata for the preflight test."
                field007:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 007 carries descriptive metadata for the preflight test."
                field008:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 008 carries descriptive metadata for the preflight test."
                field009:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 009 carries descriptive metadata for the preflight test."
                field010:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 010 carries descriptive metadata for the preflight test."
                field011:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 011 carries descriptive metadata for the preflight test."
                field012:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 012 carries descriptive metadata for the preflight test."
                field013:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 013 carries descriptive metadata for the preflight test."
                field014:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 014 carries descriptive metadata for the preflight test."
                field015:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 015 carries descriptive metadata for the preflight test."
                field016:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 016 carries descriptive metadata for the preflight test."
                field017:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 017 carries descriptive metadata for the preflight test."
                field018:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 018 carries descriptive metadata for the preflight test."
                field019:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 019 carries descriptive metadata for the preflight test."
                field020:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 020 carries descriptive metadata for the preflight test."
                field021:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 021 carries descriptive metadata for the preflight test."
                field022:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 022 carries descriptive metadata for the preflight test."
                field023:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 023 carries descriptive metadata for the preflight test."
                field024:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 024 carries descriptive metadata for the preflight test."
                field025:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 025 carries descriptive metadata for the preflight test."
                field026:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 026 carries descriptive metadata for the preflight test."
                field027:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 027 carries descriptive metadata for the preflight test."
                field028:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 028 carries descriptive metadata for the preflight test."
                field029:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 029 carries descriptive metadata for the preflight test."
                field030:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 030 carries descriptive metadata for the preflight test."
                field031:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 031 carries descriptive metadata for the preflight test."
                field032:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 032 carries descriptive metadata for the preflight test."
                field033:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 033 carries descriptive metadata for the preflight test."
                field034:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 034 carries descriptive metadata for the preflight test."
                field035:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 035 carries descriptive metadata for the preflight test."
                field036:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 036 carries descriptive metadata for the preflight test."
                field037:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 037 carries descriptive metadata for the preflight test."
                field038:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 038 carries descriptive metadata for the preflight test."
                field039:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 039 carries descriptive metadata for the preflight test."
                field040:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 040 carries descriptive metadata for the preflight test."
                field041:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 041 carries descriptive metadata for the preflight test."
                field042:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 042 carries descriptive metadata for the preflight test."
                field043:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 043 carries descriptive metadata for the preflight test."
                field044:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 044 carries descriptive metadata for the preflight test."
                field045:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 045 carries descriptive metadata for the preflight test."
                field046:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 046 carries descriptive metadata for the preflight test."
                field047:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 047 carries descriptive metadata for the preflight test."
                field048:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 048 carries descriptive metadata for the preflight test."
                field049:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 049 carries descriptive metadata for the preflight test."
                field050:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 050 carries descriptive metadata for the preflight test."
                field051:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 051 carries descriptive metadata for the preflight test."
                field052:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 052 carries descriptive metadata for the preflight test."
                field053:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 053 carries descriptive metadata for the preflight test."
                field054:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 054 carries descriptive metadata for the preflight test."
                field055:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 055 carries descriptive metadata for the preflight test."
                field056:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 056 carries descriptive metadata for the preflight test."
                field057:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 057 carries descriptive metadata for the preflight test."
                field058:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 058 carries descriptive metadata for the preflight test."
                field059:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 059 carries descriptive metadata for the preflight test."
                field060:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 060 carries descriptive metadata for the preflight test."
                field061:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 061 carries descriptive metadata for the preflight test."
                field062:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 062 carries descriptive metadata for the preflight test."
                field063:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 063 carries descriptive metadata for the preflight test."
                field064:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 064 carries descriptive metadata for the preflight test."
                field065:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 065 carries descriptive metadata for the preflight test."
                field066:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 066 carries descriptive metadata for the preflight test."
                field067:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 067 carries descriptive metadata for the preflight test."
                field068:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 068 carries descriptive metadata for the preflight test."
                field069:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 069 carries descriptive metadata for the preflight test."
                field070:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 070 carries descriptive metadata for the preflight test."
                field071:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 071 carries descriptive metadata for the preflight test."
                field072:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 072 carries descriptive metadata for the preflight test."
                field073:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 073 carries descriptive metadata for the preflight test."
                field074:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 074 carries descriptive metadata for the preflight test."
                field075:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 075 carries descriptive metadata for the preflight test."
                field076:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 076 carries descriptive metadata for the preflight test."
                field077:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 077 carries descriptive metadata for the preflight test."
                field078:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 078 carries descriptive metadata for the preflight test."
                field079:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 079 carries descriptive metadata for the preflight test."
                field080:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 080 carries descriptive metadata for the preflight test."
                field081:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 081 carries descriptive metadata for the preflight test."
                field082:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 082 carries descriptive metadata for the preflight test."
                field083:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 083 carries descriptive metadata for the preflight test."
                field084:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 084 carries descriptive metadata for the preflight test."
                field085:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 085 carries descriptive metadata for the preflight test."
                field086:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 086 carries descriptive metadata for the preflight test."
                field087:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 087 carries descriptive metadata for the preflight test."
                field088:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 088 carries descriptive metadata for the preflight test."
                field089:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 089 carries descriptive metadata for the preflight test."
                field090:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 090 carries descriptive metadata for the preflight test."
                field091:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 091 carries descriptive metadata for the preflight test."
                field092:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 092 carries descriptive metadata for the preflight test."
                field093:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 093 carries descriptive metadata for the preflight test."
                field094:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 094 carries descriptive metadata for the preflight test."
                field095:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 095 carries descriptive metadata for the preflight test."
                field096:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 096 carries descriptive metadata for the preflight test."
                field097:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 097 carries descriptive metadata for the preflight test."
                field098:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 098 carries descriptive metadata for the preflight test."
                field099:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 099 carries descriptive metadata for the preflight test."
                field100:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 100 carries descriptive metadata for the preflight test."
                field101:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 101 carries descriptive metadata for the preflight test."
                field102:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 102 carries descriptive metadata for the preflight test."
                field103:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 103 carries descriptive metadata for the preflight test."
                field104:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 104 carries descriptive metadata for the preflight test."
                field105:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 105 carries descriptive metadata for the preflight test."
                field106:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 106 carries descriptive metadata for the preflight test."
                field107:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 107 carries descriptive metadata for the preflight test."
                field108:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 108 carries descriptive metadata for the preflight test."
                field109:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 109 carries descriptive metadata for the preflight test."
                field110:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 110 carries descriptive metadata for the preflight test."
                field111:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 111 carries descriptive metadata for the preflight test."
                field112:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 112 carries descriptive metadata for the preflight test."
                field113:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 113 carries descriptive metadata for the preflight test."
                field114:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 114 carries descriptive metadata for the preflight test."
                field115:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 115 carries descriptive metadata for the preflight test."
                field116:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 116 carries descriptive metadata for the preflight test."
                field117:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 117 carries descriptive metadata for the preflight test."
                field118:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 118 carries descriptive metadata for the preflight test."
                field119:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 119 carries descriptive metadata for the preflight test."
                field120:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 120 carries descriptive metadata for the preflight test."
                field121:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 121 carries descriptive metadata for the preflight test."
                field122:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 122 carries descriptive metadata for the preflight test."
                field123:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 123 carries descriptive metadata for the preflight test."
                field124:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 124 carries descriptive metadata for the preflight test."
                field125:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 125 carries descriptive metadata for the preflight test."
                field126:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 126 carries descriptive metadata for the preflight test."
                field127:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 127 carries descriptive metadata for the preflight test."
                field128:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 128 carries descriptive metadata for the preflight test."
                field129:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 129 carries descriptive metadata for the preflight test."
                field130:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 130 carries descriptive metadata for the preflight test."
                field131:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 131 carries descriptive metadata for the preflight test."
                field132:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 132 carries descriptive metadata for the preflight test."
                field133:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 133 carries descriptive metadata for the preflight test."
                field134:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 134 carries descriptive metadata for the preflight test."
                field135:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 135 carries descriptive metadata for the preflight test."
                field136:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 136 carries descriptive metadata for the preflight test."
                field137:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 137 carries descriptive metadata for the preflight test."
                field138:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 138 carries descriptive metadata for the preflight test."
                field139:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 139 carries descriptive metadata for the preflight test."
                field140:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 140 carries descriptive metadata for the preflight test."
                field141:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 141 carries descriptive metadata for the preflight test."
                field142:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 142 carries descriptive metadata for the preflight test."
                field143:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 143 carries descriptive metadata for the preflight test."
                field144:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 144 carries descriptive metadata for the preflight test."
                field145:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 145 carries descriptive metadata for the preflight test."
                field146:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 146 carries descriptive metadata for the preflight test."
                field147:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 147 carries descriptive metadata for the preflight test."
                field148:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 148 carries descriptive metadata for the preflight test."
                field149:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 149 carries descriptive metadata for the preflight test."
                field150:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 150 carries descriptive metadata for the preflight test."
                field151:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 151 carries descriptive metadata for the preflight test."
                field152:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 152 carries descriptive metadata for the preflight test."
                field153:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 153 carries descriptive metadata for the preflight test."
                field154:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 154 carries descriptive metadata for the preflight test."
                field155:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 155 carries descriptive metadata for the preflight test."
                field156:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 156 carries descriptive metadata for the preflight test."
                field157:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 157 carries descriptive metadata for the preflight test."
                field158:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 158 carries descriptive metadata for the preflight test."
                field159:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 159 carries descriptive metadata for the preflight test."
                field160:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 160 carries descriptive metadata for the preflight test."
                field161:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 161 carries descriptive metadata for the preflight test."
                field162:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 162 carries descriptive metadata for the preflight test."
                field163:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 163 carries descriptive metadata for the preflight test."
                field164:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 164 carries descriptive metadata for the preflight test."
                field165:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 165 carries descriptive metadata for the preflight test."
                field166:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 166 carries descriptive metadata for the preflight test."
                field167:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 167 carries descriptive metadata for the preflight test."
                field168:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 168 carries descriptive metadata for the preflight test."
                field169:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 169 carries descriptive metadata for the preflight test."
                field170:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 170 carries descriptive metadata for the preflight test."
                field171:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 171 carries descriptive metadata for the preflight test."
                field172:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 172 carries descriptive metadata for the preflight test."
                field173:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 173 carries descriptive metadata for the preflight test."
                field174:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 174 carries descriptive metadata for the preflight test."
                field175:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 175 carries descriptive metadata for the preflight test."
                field176:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 176 carries descriptive metadata for the preflight test."
                field177:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 177 carries descriptive metadata for the preflight test."
                field178:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 178 carries descriptive metadata for the preflight test."
                field179:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 179 carries descriptive metadata for the preflight test."
                field180:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 180 carries descriptive metadata for the preflight test."
                field181:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 181 carries descriptive metadata for the preflight test."
                field182:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 182 carries descriptive metadata for the preflight test."
                field183:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 183 carries descriptive metadata for the preflight test."
                field184:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 184 carries descriptive metadata for the preflight test."
                field185:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 185 carries descriptive metadata for the preflight test."
                field186:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 186 carries descriptive metadata for the preflight test."
                field187:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 187 carries descriptive metadata for the preflight test."
                field188:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 188 carries descriptive metadata for the preflight test."
                field189:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 189 carries descriptive metadata for the preflight test."
                field190:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 190 carries descriptive metadata for the preflight test."
                field191:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 191 carries descriptive metadata for the preflight test."
                field192:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 192 carries descriptive metadata for the preflight test."
                field193:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 193 carries descriptive metadata for the preflight test."
                field194:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 194 carries descriptive metadata for the preflight test."
                field195:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 195 carries descriptive metadata for the preflight test."
                field196:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 196 carries descriptive metadata for the preflight test."
                field197:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 197 carries descriptive metadata for the preflight test."
                field198:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 198 carries descriptive metadata for the preflight test."
                field199:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 199 carries descriptive metadata for the preflight test."
                field200:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 200 carries descriptive metadata for the preflight test."
                field201:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 201 carries descriptive metadata for the preflight test."
                field202:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 202 carries descriptive metadata for the preflight test."
                field203:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 203 carries descriptive metadata for the preflight test."
                field204:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 204 carries descriptive metadata for the preflight test."
                field205:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 205 carries descriptive metadata for the preflight test."
                field206:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 206 carries descriptive metadata for the preflight test."
                field207:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 207 carries descriptive metadata for the preflight test."
                field208:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 208 carries descriptive metadata for the preflight test."
                field209:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 209 carries descriptive metadata for the preflight test."
                field210:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 210 carries descriptive metadata for the preflight test."
                field211:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 211 carries descriptive metadata for the preflight test."
                field212:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 212 carries descriptive metadata for the preflight test."
                field213:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 213 carries descriptive metadata for the preflight test."
                field214:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 214 carries descriptive metadata for the preflight test."
                field215:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 215 carries descriptive metadata for the preflight test."
                field216:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 216 carries descriptive metadata for the preflight test."
                field217:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 217 carries descriptive metadata for the preflight test."
                field218:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 218 carries descriptive metadata for the preflight test."
                field219:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 219 carries descriptive metadata for the preflight test."
                field220:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 220 carries descriptive metadata for the preflight test."
                field221:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 221 carries descriptive metadata for the preflight test."
                field222:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 222 carries descriptive metadata for the preflight test."
                field223:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 223 carries descriptive metadata for the preflight test."
                field224:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 224 carries descriptive metadata for the preflight test."
                field225:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 225 carries descriptive metadata for the preflight test."
                field226:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 226 carries descriptive metadata for the preflight test."
                field227:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 227 carries descriptive metadata for the preflight test."
                field228:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 228 carries descriptive metadata for the preflight test."
                field229:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 229 carries descriptive metadata for the preflight test."
                field230:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 230 carries descriptive metadata for the preflight test."
                field231:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 231 carries descriptive metadata for the preflight test."
                field232:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 232 carries descriptive metadata for the preflight test."
                field233:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 233 carries descriptive metadata for the preflight test."
                field234:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 234 carries descriptive metadata for the preflight test."
                field235:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 235 carries descriptive metadata for the preflight test."
                field236:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 236 carries descriptive metadata for the preflight test."
                field237:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 237 carries descriptive metadata for the preflight test."
                field238:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 238 carries descriptive metadata for the preflight test."
                field239:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 239 carries descriptive metadata for the preflight test."
                field240:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 240 carries descriptive metadata for the preflight test."
                field241:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 241 carries descriptive metadata for the preflight test."
                field242:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 242 carries descriptive metadata for the preflight test."
                field243:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 243 carries descriptive metadata for the preflight test."
                field244:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 244 carries descriptive metadata for the preflight test."
                field245:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 245 carries descriptive metadata for the preflight test."
                field246:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 246 carries descriptive metadata for the preflight test."
                field247:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 247 carries descriptive metadata for the preflight test."
                field248:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 248 carries descriptive metadata for the preflight test."
                field249:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 249 carries descriptive metadata for the preflight test."
                field250:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 250 carries descriptive metadata for the preflight test."
                field251:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 251 carries descriptive metadata for the preflight test."
                field252:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 252 carries descriptive metadata for the preflight test."
                field253:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 253 carries descriptive metadata for the preflight test."
                field254:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 254 carries descriptive metadata for the preflight test."
                field255:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 255 carries descriptive metadata for the preflight test."
                field256:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 256 carries descriptive metadata for the preflight test."
                field257:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 257 carries descriptive metadata for the preflight test."
                field258:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 258 carries descriptive metadata for the preflight test."
                field259:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 259 carries descriptive metadata for the preflight test."
                field260:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 260 carries descriptive metadata for the preflight test."
                field261:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 261 carries descriptive metadata for the preflight test."
                field262:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 262 carries descriptive metadata for the preflight test."
                field263:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 263 carries descriptive metadata for the preflight test."
                field264:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 264 carries descriptive metadata for the preflight test."
                field265:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 265 carries descriptive metadata for the preflight test."
                field266:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 266 carries descriptive metadata for the preflight test."
                field267:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 267 carries descriptive metadata for the preflight test."
                field268:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 268 carries descriptive metadata for the preflight test."
                field269:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 269 carries descriptive metadata for the preflight test."
                field270:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 270 carries descriptive metadata for the preflight test."
                field271:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 271 carries descriptive metadata for the preflight test."
                field272:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 272 carries descriptive metadata for the preflight test."
                field273:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 273 carries descriptive metadata for the preflight test."
                field274:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 274 carries descriptive metadata for the preflight test."
                field275:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 275 carries descriptive metadata for the preflight test."
                field276:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 276 carries descriptive metadata for the preflight test."
                field277:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 277 carries descriptive metadata for the preflight test."
                field278:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 278 carries descriptive metadata for the preflight test."
                field279:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 279 carries descriptive metadata for the preflight test."
                field280:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 280 carries descriptive metadata for the preflight test."
                field281:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 281 carries descriptive metadata for the preflight test."
                field282:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 282 carries descriptive metadata for the preflight test."
                field283:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 283 carries descriptive metadata for the preflight test."
                field284:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 284 carries descriptive metadata for the preflight test."
                field285:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 285 carries descriptive metadata for the preflight test."
                field286:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 286 carries descriptive metadata for the preflight test."
                field287:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 287 carries descriptive metadata for the preflight test."
                field288:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 288 carries descriptive metadata for the preflight test."
                field289:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 289 carries descriptive metadata for the preflight test."
                field290:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 290 carries descriptive metadata for the preflight test."
                field291:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 291 carries descriptive metadata for the preflight test."
                field292:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 292 carries descriptive metadata for the preflight test."
                field293:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 293 carries descriptive metadata for the preflight test."
                field294:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 294 carries descriptive metadata for the preflight test."
                field295:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 295 carries descriptive metadata for the preflight test."
                field296:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 296 carries descriptive metadata for the preflight test."
                field297:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 297 carries descriptive metadata for the preflight test."
                field298:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 298 carries descriptive metadata for the preflight test."
                field299:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 299 carries descriptive metadata for the preflight test."
                field300:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 300 carries descriptive metadata for the preflight test."
                field301:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 301 carries descriptive metadata for the preflight test."
                field302:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 302 carries descriptive metadata for the preflight test."
                field303:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 303 carries descriptive metadata for the preflight test."
                field304:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 304 carries descriptive metadata for the preflight test."
                field305:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 305 carries descriptive metadata for the preflight test."
                field306:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 306 carries descriptive metadata for the preflight test."
                field307:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 307 carries descriptive metadata for the preflight test."
                field308:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 308 carries descriptive metadata for the preflight test."
                field309:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 309 carries descriptive metadata for the preflight test."
                field310:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 310 carries descriptive metadata for the preflight test."
                field311:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 311 carries descriptive metadata for the preflight test."
                field312:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 312 carries descriptive metadata for the preflight test."
                field313:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 313 carries descriptive metadata for the preflight test."
                field314:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 314 carries descriptive metadata for the preflight test."
                field315:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 315 carries descriptive metadata for the preflight test."
                field316:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 316 carries descriptive metadata for the preflight test."
                field317:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 317 carries descriptive metadata for the preflight test."
                field318:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 318 carries descriptive metadata for the preflight test."
                field319:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 319 carries descriptive metadata for the preflight test."
                field320:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 320 carries descriptive metadata for the preflight test."
                field321:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 321 carries descriptive metadata for the preflight test."
                field322:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 322 carries descriptive metadata for the preflight test."
                field323:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 323 carries descriptive metadata for the preflight test."
                field324:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 324 carries descriptive metadata for the preflight test."
                field325:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 325 carries descriptive metadata for the preflight test."
                field326:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 326 carries descriptive metadata for the preflight test."
                field327:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 327 carries descriptive metadata for the preflight test."
                field328:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 328 carries descriptive metadata for the preflight test."
                field329:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 329 carries descriptive metadata for the preflight test."
                field330:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 330 carries descriptive metadata for the preflight test."
                field331:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 331 carries descriptive metadata for the preflight test."
                field332:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 332 carries descriptive metadata for the preflight test."
                field333:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 333 carries descriptive metadata for the preflight test."
                field334:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 334 carries descriptive metadata for the preflight test."
                field335:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 335 carries descriptive metadata for the preflight test."
                field336:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 336 carries descriptive metadata for the preflight test."
                field337:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 337 carries descriptive metadata for the preflight test."
                field338:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 338 carries descriptive metadata for the preflight test."
                field339:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 339 carries descriptive metadata for the preflight test."
                field340:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 340 carries descriptive metadata for the preflight test."
                field341:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 341 carries descriptive metadata for the preflight test."
                field342:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 342 carries descriptive metadata for the preflight test."
                field343:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 343 carries descriptive metadata for the preflight test."
                field344:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 344 carries descriptive metadata for the preflight test."
                field345:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 345 carries descriptive metadata for the preflight test."
                field346:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 346 carries descriptive metadata for the preflight test."
                field347:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 347 carries descriptive metadata for the preflight test."
                field348:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 348 carries descriptive metadata for the preflight test."
                field349:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 349 carries descriptive metadata for the preflight test."
                field350:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 350 carries descriptive metadata for the preflight test."
                field351:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 351 carries descriptive metadata for the preflight test."
                field352:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 352 carries descriptive metadata for the preflight test."
                field353:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 353 carries descriptive metadata for the preflight test."
                field354:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 354 carries descriptive metadata for the preflight test."
                field355:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 355 carries descriptive metadata for the preflight test."
                field356:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 356 carries descriptive metadata for the preflight test."
                field357:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 357 carries descriptive metadata for the preflight test."
                field358:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 358 carries descriptive metadata for the preflight test."
                field359:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 359 carries descriptive metadata for the preflight test."
                field360:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 360 carries descriptive metadata for the preflight test."
                field361:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 361 carries descriptive metadata for the preflight test."
                field362:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 362 carries descriptive metadata for the preflight test."
                field363:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 363 carries descriptive metadata for the preflight test."
                field364:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 364 carries descriptive metadata for the preflight test."
                field365:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 365 carries descriptive metadata for the preflight test."
                field366:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 366 carries descriptive metadata for the preflight test."
                field367:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 367 carries descriptive metadata for the preflight test."
                field368:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 368 carries descriptive metadata for the preflight test."
                field369:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 369 carries descriptive metadata for the preflight test."
                field370:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 370 carries descriptive metadata for the preflight test."
                field371:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 371 carries descriptive metadata for the preflight test."
                field372:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 372 carries descriptive metadata for the preflight test."
                field373:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 373 carries descriptive metadata for the preflight test."
                field374:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 374 carries descriptive metadata for the preflight test."
                field375:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 375 carries descriptive metadata for the preflight test."
                field376:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 376 carries descriptive metadata for the preflight test."
                field377:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 377 carries descriptive metadata for the preflight test."
                field378:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 378 carries descriptive metadata for the preflight test."
                field379:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 379 carries descriptive metadata for the preflight test."
                field380:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 380 carries descriptive metadata for the preflight test."
                field381:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 381 carries descriptive metadata for the preflight test."
                field382:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 382 carries descriptive metadata for the preflight test."
                field383:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 383 carries descriptive metadata for the preflight test."
                field384:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 384 carries descriptive metadata for the preflight test."
                field385:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 385 carries descriptive metadata for the preflight test."
                field386:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 386 carries descriptive metadata for the preflight test."
                field387:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 387 carries descriptive metadata for the preflight test."
                field388:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 388 carries descriptive metadata for the preflight test."
                field389:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 389 carries descriptive metadata for the preflight test."
                field390:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 390 carries descriptive metadata for the preflight test."
                field391:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 391 carries descriptive metadata for the preflight test."
                field392:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 392 carries descriptive metadata for the preflight test."
                field393:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 393 carries descriptive metadata for the preflight test."
                field394:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 394 carries descriptive metadata for the preflight test."
                field395:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 395 carries descriptive metadata for the preflight test."
                field396:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 396 carries descriptive metadata for the preflight test."
                field397:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 397 carries descriptive metadata for the preflight test."
                field398:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 398 carries descriptive metadata for the preflight test."
                field399:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 399 carries descriptive metadata for the preflight test."
                field400:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 400 carries descriptive metadata for the preflight test."
                field401:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 401 carries descriptive metadata for the preflight test."
                field402:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 402 carries descriptive metadata for the preflight test."
                field403:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 403 carries descriptive metadata for the preflight test."
                field404:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 404 carries descriptive metadata for the preflight test."
                field405:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 405 carries descriptive metadata for the preflight test."
                field406:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 406 carries descriptive metadata for the preflight test."
                field407:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 407 carries descriptive metadata for the preflight test."
                field408:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 408 carries descriptive metadata for the preflight test."
                field409:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 409 carries descriptive metadata for the preflight test."
                field410:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 410 carries descriptive metadata for the preflight test."
                field411:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 411 carries descriptive metadata for the preflight test."
                field412:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 412 carries descriptive metadata for the preflight test."
                field413:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 413 carries descriptive metadata for the preflight test."
                field414:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 414 carries descriptive metadata for the preflight test."
                field415:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 415 carries descriptive metadata for the preflight test."
                field416:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 416 carries descriptive metadata for the preflight test."
                field417:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 417 carries descriptive metadata for the preflight test."
                field418:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 418 carries descriptive metadata for the preflight test."
                field419:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 419 carries descriptive metadata for the preflight test."
                field420:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 420 carries descriptive metadata for the preflight test."
                field421:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 421 carries descriptive metadata for the preflight test."
                field422:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 422 carries descriptive metadata for the preflight test."
                field423:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 423 carries descriptive metadata for the preflight test."
                field424:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 424 carries descriptive metadata for the preflight test."
                field425:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 425 carries descriptive metadata for the preflight test."
                field426:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 426 carries descriptive metadata for the preflight test."
                field427:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 427 carries descriptive metadata for the preflight test."
                field428:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 428 carries descriptive metadata for the preflight test."
                field429:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 429 carries descriptive metadata for the preflight test."
                field430:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 430 carries descriptive metadata for the preflight test."
                field431:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 431 carries descriptive metadata for the preflight test."
                field432:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 432 carries descriptive metadata for the preflight test."
                field433:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 433 carries descriptive metadata for the preflight test."
                field434:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 434 carries descriptive metadata for the preflight test."
                field435:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 435 carries descriptive metadata for the preflight test."
                field436:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 436 carries descriptive metadata for the preflight test."
                field437:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 437 carries descriptive metadata for the preflight test."
                field438:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 438 carries descriptive metadata for the preflight test."
                field439:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 439 carries descriptive metadata for the preflight test."
                field440:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 440 carries descriptive metadata for the preflight test."
                field441:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 441 carries descriptive metadata for the preflight test."
                field442:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 442 carries descriptive metadata for the preflight test."
                field443:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 443 carries descriptive metadata for the preflight test."
                field444:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 444 carries descriptive metadata for the preflight test."
                field445:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 445 carries descriptive metadata for the preflight test."
                field446:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 446 carries descriptive metadata for the preflight test."
                field447:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 447 carries descriptive metadata for the preflight test."
                field448:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 448 carries descriptive metadata for the preflight test."
                field449:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 449 carries descriptive metadata for the preflight test."
                field450:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 450 carries descriptive metadata for the preflight test."
                field451:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 451 carries descriptive metadata for the preflight test."
                field452:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 452 carries descriptive metadata for the preflight test."
                field453:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 453 carries descriptive metadata for the preflight test."
                field454:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 454 carries descriptive metadata for the preflight test."
                field455:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 455 carries descriptive metadata for the preflight test."
                field456:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 456 carries descriptive metadata for the preflight test."
                field457:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 457 carries descriptive metadata for the preflight test."
                field458:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 458 carries descriptive metadata for the preflight test."
                field459:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 459 carries descriptive metadata for the preflight test."
                field460:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 460 carries descriptive metadata for the preflight test."
                field461:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 461 carries descriptive metadata for the preflight test."
                field462:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 462 carries descriptive metadata for the preflight test."
                field463:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 463 carries descriptive metadata for the preflight test."
                field464:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 464 carries descriptive metadata for the preflight test."
                field465:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 465 carries descriptive metadata for the preflight test."
                field466:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 466 carries descriptive metadata for the preflight test."
                field467:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 467 carries descriptive metadata for the preflight test."
                field468:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 468 carries descriptive metadata for the preflight test."
                field469:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 469 carries descriptive metadata for the preflight test."
                field470:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 470 carries descriptive metadata for the preflight test."
                field471:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 471 carries descriptive metadata for the preflight test."
                field472:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 472 carries descriptive metadata for the preflight test."
                field473:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 473 carries descriptive metadata for the preflight test."
                field474:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 474 carries descriptive metadata for the preflight test."
                field475:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 475 carries descriptive metadata for the preflight test."
                field476:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 476 carries descriptive metadata for the preflight test."
                field477:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 477 carries descriptive metadata for the preflight test."
                field478:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 478 carries descriptive metadata for the preflight test."
                field479:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 479 carries descriptive metadata for the preflight test."
                field480:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 480 carries descriptive metadata for the preflight test."
                field481:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 481 carries descriptive metadata for the preflight test."
                field482:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 482 carries descriptive metadata for the preflight test."
                field483:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 483 carries descriptive metadata for the preflight test."
                field484:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 484 carries descriptive metadata for the preflight test."
                field485:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 485 carries descriptive metadata for the preflight test."
                field486:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 486 carries descriptive metadata for the preflight test."
                field487:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 487 carries descriptive metadata for the preflight test."
                field488:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 488 carries descriptive metadata for the preflight test."
                field489:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 489 carries descriptive metadata for the preflight test."
                field490:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 490 carries descriptive metadata for the preflight test."
                field491:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 491 carries descriptive metadata for the preflight test."
                field492:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 492 carries descriptive metadata for the preflight test."
                field493:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 493 carries descriptive metadata for the preflight test."
                field494:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 494 carries descriptive metadata for the preflight test."
                field495:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 495 carries descriptive metadata for the preflight test."
                field496:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 496 carries descriptive metadata for the preflight test."
                field497:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 497 carries descriptive metadata for the preflight test."
                field498:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 498 carries descriptive metadata for the preflight test."
                field499:
                  type: string
                  description: "Stores a platform resource used by the mini-kubara case-02 large-CRD fixture. This description is deliberately verbose so that the composed CRD body, when JSON-encoded by kubectl client-side apply and stamped into the last-applied-configuration annotation, exceeds the Kubernetes 262144-byte metadata.annotations limit. The fixture lets preflight tooling detect the risk before any live apply, and forces the delivery mode decision to become explicit. Field index 499 carries descriptive metadata for the preflight test."
            status:
              type: object
              properties:
                phase:
                  type: string
                  description: "Observed phase of the Store object."
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: Phase
          type: string
          jsonPath: .status.phase

# Prompt Seeds

## Explain The Example

Explain what `incubator/custom-ownership-detectors` demonstrates, using the files in this directory only. Be explicit about what mutates ConfigHub, what mutates live infrastructure, and what evidence is generated locally.

## Run Safely

Drive `incubator/custom-ownership-detectors` in the safest order. Start with read-only commands, then create the fixture cluster, then verify custom ownership in `map` and capture the current `explain` and `trace` limitation.

## Summarize The Evidence

Read `sample-output/map.json`, `sample-output/payments-api.explain.json`, `sample-output/infra-ui.explain.json`, and `sample-output/payments-api.trace.json`. Summarize what they prove, what the current parity gap is, and why this example still matters for platform teams.

# Prompt Seeds

## Explain The Example

Explain what `incubator/fleet-import` demonstrates, using the files in this directory only. Be explicit about what mutates ConfigHub, what mutates live infrastructure, and what the fleet proposal proves.

## Run Safely

Drive `incubator/fleet-import` in the safest order. Start with read-only commands, then generate the local fleet aggregation output, then verify it against the committed expected output.

## Summarize The Evidence

Read `sample-output/fleet-summary.json` after `./setup.sh`. Summarize what it proves, what it does not prove, and why this example matters after the single-cluster import examples.

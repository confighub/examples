# Prompt Seeds

## Explain The Example

Explain what `incubator/demo-data-adt` demonstrates, using the files in this directory only. Be explicit about what mutates ConfigHub, what mutates live infrastructure, and what the scan results prove.

## Run Safely

Drive `incubator/demo-data-adt` in the safest order. Start with read-only commands, then generate the local scan outputs, then verify them against the committed expected outputs.

## Summarize The Evidence

Read the three JSON files under `sample-output/` after `./setup.sh`. Summarize what they prove, what they do not prove, and how this example relates to App-Deployment-Target labeling and the stable `promotion-demo-data` example.

# Prompt Seeds

## Explain The Example

Explain what `graph-export` demonstrates, using the files in this directory only. Be explicit about what mutates ConfigHub, what mutates live infrastructure, and what artifacts are generated locally.

## Run Safely

Drive `graph-export` in the safest order. Start with read-only commands, then create the fixture cluster, then verify the exported graph artifacts.

## Summarize The Evidence

Read `sample-output/graph.json`, `sample-output/graph.dot`, `sample-output/graph.svg`, and `sample-output/graph.html`. Summarize what they prove, what they do not prove, and why this example matters for evidence-first workflows.

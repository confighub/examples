# Prompt Seeds

## Explain The Example

Explain what `incubator/connect-and-compare` demonstrates, using the files in this directory only. Be explicit about what mutates ConfigHub, what mutates live infrastructure, and what evidence is generated locally.

## Run Safely

Drive `incubator/connect-and-compare` in the safest order. Start with read-only commands, then generate local sample output, then verify it against the committed expected output.

## Summarize The Evidence

Read `sample-output/01-doctor.txt`, `sample-output/03-compare.json`, and `sample-output/04-history.txt`. Summarize what they prove, what they do not prove, and why this example is useful in the current GitHub plus GitOps plus ConfigHub wedge.

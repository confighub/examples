# Prompt Seeds

## Explain The Example

Explain what `incubator/orphans` demonstrates, using the files in this directory only. Be explicit about what mutates ConfigHub, what mutates live infrastructure, and what evidence is generated locally.

## Run Safely

Drive `incubator/orphans` in the safest order. Start with read-only commands, then create the fixture cluster, then verify the orphan inventory and the captured native trace result.

## Summarize The Evidence

Read `sample-output/orphans.json` and `sample-output/debug-nginx.trace.json`. Summarize what they prove, what they do not prove, and why this example matters for brownfield operator work.

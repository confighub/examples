# Prompt Seeds

## Explain The Example

Explain what `incubator/import-from-bundle` demonstrates, using the files in this directory only. Be explicit about what mutates ConfigHub, what mutates live infrastructure, and what the dry-run proposal proves.

## Run Safely

Drive `incubator/import-from-bundle` in the safest order. Start with read-only commands, then generate the local dry-run proposal, then verify it against the committed expected output.

## Summarize The Evidence

Read `sample-output/suggestion.json` after `./setup.sh`. Summarize what it proves, what it does not prove, and why this is useful alongside the live Argo and Flux import examples.

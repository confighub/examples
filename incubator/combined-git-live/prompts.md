# Prompt Seeds

## Explain The Example

Explain what `incubator/combined-git-live` demonstrates, using the files in this directory only. Be explicit about what mutates ConfigHub, what mutates live infrastructure, and what the compare result proves.

## Run Safely

Drive `incubator/combined-git-live` in the safest order. Start with read-only commands, then apply the cluster fixtures, then run the compare verification, then summarize the aligned, git-only, and cluster-only findings.

## Summarize The Evidence

Read `sample-output/alignment.json` after `./verify.sh` and summarize what it proves, what it does not prove, and why this example matters after `connect-and-compare`.

apiVersion: v1
kind: Service
metadata:
  name: inventory-api
  namespace: apps
spec:
  selector:
    app.kubernetes.io/name: inventory-api
  ports:
    - name: http
      port: 80
      targetPort: 8081

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The tenant owns this folder. The platform team owns everything above it.
resources:
  - deployment.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: team-checkout

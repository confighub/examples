apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Renders the hand-applied root Application. The objects the root
# Application then creates live in children/.
resources:
  - root-app.yaml

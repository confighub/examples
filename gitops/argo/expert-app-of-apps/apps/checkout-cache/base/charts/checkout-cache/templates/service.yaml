apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app.kubernetes.io/name: checkout-cache
    app.kubernetes.io/instance: {{ .Release.Name }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: checkout-cache
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: cache
      port: {{ .Values.service.port }}
      targetPort: cache

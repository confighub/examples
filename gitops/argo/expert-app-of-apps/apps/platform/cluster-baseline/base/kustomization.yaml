apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - quota.yaml

labels:
  - pairs:
      app.kubernetes.io/name: cluster-baseline
      app.kubernetes.io/part-of: platform
